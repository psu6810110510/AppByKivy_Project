ภาพรวมของโปรแกรม การทำงานใน Code และ การรันตัวเกม Project : Kivy Sudoku 
1. ภาพรวมของโปรแกรม 
Kivy Sudoku  คือแอปพลิเคชันเกมลับสมองซูโดกุที่พัฒนาด้วยภาษา Python โดยใช้เฟรมเวิร์ก Kivy ในการสร้างหน้าจอแสดงผล โปรแกรมนี้ออกแบบมาตามหลักการแยกส่วนการทำงาน  ระหว่างส่วนตรรกะการคำนวณ(Backend) และส่วนแสดงผล(Frontend) เพื่อความง่ายในการพัฒนาและเพิ่มฟีเจอร์ในอนาคต

2. ฟีเจอร์ที่โดดเด่น 
2.1 Difficulty Algorithm หรือ ระบบสุ่มโจทย์ตามระดับความยาก (Easy, Medium, Hard) โดยใช้ Algorithm ในการลบตัวเลขออกจากกระดานที่สมบูรณ์แล้ว 
2.2 Pencil Mode (Note)ระบบจดบันทึกตัวเลขขนาดเล็กในช่องว่าง 
2.3 Toggle สลับเปิด/ปิดเลขทดสอบ ช่วยให้ผู้เล่นวิเคราะห์ความเป็นไปได้หลายตัวเลข
2.4 ระบบย้อนกลับและทำซ้ำการเดินผ่าน Stack เพื่อความผิดพลาดที่สามารถแก้ไขได้ทุกขั้นตอน
2.5 Live Validation หรือ ระบบตรวจสอบตัวเลขแบบเรียลไทม์: สีเขียว (ถูกต้อง), สีแดง (ผิดกติกา) และ สีฟ้า (คำใบ้/Hint)
2.6 Data Persistence หรือ ระบบบันทึกเกม (Save/Load) คะแนนสถิติ ลงในไฟล์ .json
2.7 Responsive UI หรือ หน้าจอสลับระหว่างเมนูหลักและหน้าเล่นเกมได้ลื่นไหลด้วย ScreenManager

3. รายละเอียดเชิงเทคนิค 
3.1. Widgets มากกว่า 30 Widgets โดยโปรแกรมมีการใช้งาน Widget หลากหลายประเภท รวมแล้วเกิน 80 ชิ้น
3.1.1 TextInput (Sudoku Cells): จัดวางใน GridLayout (9x9) เพื่อรับข้อมูลจากผู้เล่น
3.1.2 Button: สำหรับควบคุมระบบต่างๆ เช่น Restart, Hint, Save, Load, Undo, Redo1
3.1.3 Multiple BoxLayout & GridLayout: ใช้ซ้อนกันเพื่อจัดระเบียบโครงสร้าง UI 
3.1.4 Label ทำ Stats Display : ใช้แสดงคะแนน เวลา และสถิติสูงสุดแบบสีสันสดใสด้วย MarkupPopup: สำหรับแจ้งเตือนชัยชนะ
3.2. Callbacks
มีการดักจับ Event สำคัญผ่านการใช้ bind และฟังก์ชันจัดการดังนี้
check_answer: ทำงานเมื่อมีการเปลี่ยนแปลงข้อความในช่องตาราง
start_new_game: เริ่มเกมใหม่และสุ่มโจทย์ตามระดับที่เลือก
toggle_note: สลับโหมดการป้อนข้อมูลระหว่าง Note และ Answer
undo_move: ย้อนกลับสถานะกระดานล่าสุด
redo_move: เดินหน้าไปยังสถานะที่เคยย้อนกลับไป
save_game: เขียนข้อมูลสถานะปัจจุบันลงไฟล์ JSON
load_game: อ่านไฟล์ JSON และกู้คืนกระดาน
give_hint: ระบบค้นหาตัวเลขที่ถูกต้องจาก Solution และเติมให้ผู้เล่น
clear_game: ล้างกระดานและรีเซ็ตเวลา
update_timer: ฟังก์ชัน Callback ที่ทำงานทุก 1 วินาทีผ่าน Clock.schedule_interval
go_to_menu: ระบบสลับหน้าจอกลับไปยังหน้าหลัก

4. อธิบายการทำงานของโค้ด
4.1 ตรรกะเบื้องหลัง (Backend Logic)
SudokuEngine: ทำหน้าที่สร้างกระดานที่ถูกต้อง โดยใช้หลักการ Recursive Backtracking และลบตัวเลขออกตามระดับความยากที่ผู้เล่นเลือก
Validation: ทุกครั้งที่มีการป้อนเลข ระบบจะเรียกฟังก์ชัน check_move เพื่อตรวจสอบว่าเลขนั้นซ้ำในแถว คอลัมน์ หรือบล็อก 3x3 หรือไม่
4.2 ส่วนติดต่อผู้ใช้ (Frontend Logic)
TextInput Binding: ทุกช่อง TextInput จะถูกผูกไว้กับฟังก์ชัน check_answer เมื่อผู้เล่นพิมพ์เลข โปรแกรมจะวิเคราะห์ทันทีว่าเป็น Note หรือ Answer แล้วจึงแสดงผลสีหรือขนาดฟอนต์ให้ถูกต้อง
Screen Management: ใช้ ScreenManager ในการจัดการลำดับชั้นของหน้าต่าง ทำให้แอปพลิเคชันดูเป็นระบบเหมือนแอปมือถือจริง

5. วิธีการติดตั้งและใช้งาาน
เตรียมสภาพแวดล้อม: ติดตั้ง Python (3.7+) และ Library => Kivy Bash pip install kivy
ดาวน์โหลดโค้ด: โคลนโปรเจกต์จาก Git Bash git clone [Your-Git-URL]
การรันโปรแกรม: Bash python Frontend/main.py

6. แนวทางการจัดการทีมและ Git
ทำกันวันที่ 23 ก.พ.69 - 8 มี.ค. 69

สมาชิกในทีมแบ่งงานกันดังนี้ 

สัปดาห์ที่ 1: ส่วนใหญ่ทำรากฐานและการคำนวณ และ หน้า UI พื้นฐาน
Member 1 6810110009 นายกฤษฏิ์ รัตนอุบล (Commits 3-4,9-14,23-28):
1.
2.

Member 2 6810110198 นายปัณณวัฒน์ กุลรังษี (Commits 6-8,22):
1.แก้ไข README.md ในส่วนต่างๆ
2.อ่าน Frontend เดิมในส่วนของเพื่อนที่มีอยู่เพื่อทำความเข้าใจระบบ Frontend ปัจจุบัน
3.เพิ่มฟังก์ชัน Timer : ระบบนับเวลาเดินหน้า
4.เพิ่มฟังก์ชัน Scoring System : คำนวณคะแนนแบบ Real-time
5.ตรวจสอบการใช้งานของฟังก์ชัน Timer & Scoring System ตรวจสอบ Bug และ Error
6.เพิ่มฟังก์ชัน Save/Load System: ใช้ JsonStore ของ Kivy เพื่อบันทึกเกมที่เล่นค้างไว้ (เซฟสถานะกระดาน เวลา และคะแนน)
7.ตรวจสอบการใช้งานของฟังก์ชัน Save/Load System ใช้งานได้ไหม
8.ทดสอบระบบเกมเบื้องต้น พื้นฐาน

Member 3 6810110510 ศิรสิทธิ์ ศุภสาร (Commits 1-2,5,15-21,29): 
1.สร้าง reposititory ใน github  
2.จัดการ README.md ในโปรเจก(ส่วนของfrontend และ การทำงานของโปรแกรม(หน้าข้อความที่กำลังอ่านอยู่นี้))
3.แบ่งส่วน ในโฟลเดอร์ เป็น backend และ frontend
4.จัดการ gitinore(ห้าม venv และ อื่นๆ)
5.เพิ่ม main.py ใน frontend
6.สร้าง class SudokuBoard ใน frontend
6.1.สร้างและตั้งค่าพื้นฐาน ให้ตารางใน ใน class SudokuBoard สามารถแสดงผลและใส่ตัวเลขได้ ใน frontend
7ตั้งค่าขนาดหน้าจอเป็น window size ใน frontend
8.สร้าง class SudokuApp ใน frontend
8.1ตั้งค่า class SudokuApp การจัดวาง layout  ใน frontend
8.2สร้างปุ่ม clear(เปลี่ยนเป็น ปุ่ม restart ในภายหลัง) และ  ปุ่ม solve(ภายหลังเอาออก และ พัฒนาเป็น ปุ่มhint แทน) และทำให้มันใช้งานได้ ใน frontend
9.กำหนดส่วนการเริ่มรันโปรแกรม

สัปดาห์ที่ 2: ส่วนใหญ่ทำฟีเจอร์และ UI
Member 1 6810110009 นายกฤษฏิ์ รัตนอุบล (Commits 34-39,61-66) รวม 26 commit: 
1.

Member 2 6810110198 นายปัณณวัฒน์ กุลรังษี (Commits 30-33,43-60) รวม 26 commit :
1.เพิ่มฟังก์ชัน Hint System : ดึง Logic จากหลังบ้านมาทำปุ่ม "คำใบ้" ซึ่งกดหนึ่งครั้งละลบ 20 คะแนน
2.เพิ่มฟังก์ชัน Win Detection : เพิ่มระบบตรวจจับการจบเกมพร้อมโชว์ Popup สรุปผลเมื่อชนะ
3.ตรวจสอบการใช้งานของฟังก์ชัน Hint System & Win Detection
4.เพิ่มฟังก์ชัน Input Validation : ดักจับ Error (เช่น ห้ามใส่ตัวอักษรหรือเลขเกิน) 
5.เพิ่มฟังก์ชัน Undo/Redo : และสร้างระบบย้อนกลับ/ทำซ้ำการเดิน(Undo/Redo)
6.ตรวจสอบการใช้งานของฟังก์ชัน Input Validation & Undo/Redo
7.ทดสอบเกมเล่นได้จริงไหม ตรวจสอบ Bug และ Error
8.แก้ไข README.md ในส่วนต่างๆและสรุปผลอธิบายการทำงานของระบบเกม

Member 3 6810110510 ศิรสิทธิ์ ศุภสาร (Commits 40-42,67-83) รวม 31 commit: 
1.จัดการ README.md ในโปรเจก(frontend และ การทำงานของโปรแกรม(หน้าข้อความที่กำลังอ่านอยู่นี้))
2.สร้างปุ่ม new game และ ทำให้ใช้งานได้ ใน frontend
3.สร้าง การตรวจสอบการชนะ เมื่อเติมเลขครบทุกตัวและถูกต้อง และ ทำให้ใช้งานได้ ใน frontend
4.สร้างปุ่ม เลือกความยากทั้ง 3 ปุ่ม และ ทำให้ใช้งานได้ ใน frontend
5.สร้างปุ่ม กลับหน้าหลัก และ ทำให้ใช้งานได้ ใน frontend
6.เลือก เปลี่ยนมาใช้ScreenManager() ในการควบคุมหน้าจอทั้งหมด และทำการปรับปรุงหน้าหลัก ใน frontend
7.ตกแต่งหน้า frontend ให้ดูสวยงาม และหน้าใช้งานมากขึ้้น 
7.1เปลี่ยน background coler ดูสบายตา
7.2เปลี่ยน front stye  ให้ดู modern และ ใหญ่ขึ้น
7.3เปลี่ยนสีที่ข้อความ ปุ่มต่างๆเกือบทั้งหมด เช่น ปุ่มเลือกความยากเป็นเขียว เหลือง แดง ปุ่มredo undo เป็นสีพาสเทล ,score เป็นสีทอง เป็นต้น
7.4เปลี่ยนปุ่ม new game เป็นปุ่ม restart ใน frontend
8 เพิ่มฟังชันการนับสกอร์สูงสุด และ ทำให้ใช้งานได้ ใน frontend
9.เพิ่มปุ่มการทดเลข โดยปุ่มสามรถกดเปิดปิดได้ มีอยู่และทำงานได้ใน frontend


สรุป commit
Member 1 6810110009 นายกฤษฏิ์ รัตนอุบล รวม 26 commit
Member 2 6810110198 นายปัณณวัฒน์ กุลรังษี รวม 27 commit
Member 3 6810110510 ศิรสิทธิ์ ศุภสาร  รวม 31 commit
